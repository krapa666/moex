apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moex-ingress
  namespace: moex
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: moex.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
